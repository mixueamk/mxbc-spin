<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>蜜雪冰城 第二杯半价券 | MIXUE 2nd Cup 50% Off</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: linear-gradient(135deg, #ff4d4d 0%, #ff7777 100%);
            font-family: 'Arial', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 20px;
        }
        .coupon {
            background: white;
            border-radius: 24px;
            width: 100%;
            max-width: 380px;
            overflow: hidden;
            box-shadow: 0 12px 40px rgba(0,0,0,0.3);
            text-align: center;
        }
        /* 顶部红色区域 */
        .coupon-header {
            background: linear-gradient(180deg, #e63946 0%, #c0392b 100%);
            padding: 28px 20px 22px;
            position: relative;
        }
        .coupon-header .brand {
            font-size: 26px;
            font-weight: bold;
            color: white;
            text-shadow: 2px 2px 0 #a71d2a;
        }
        .coupon-header .brand-en {
            font-size: 12px;
            color: #ffcccc;
            display: block;
            letter-spacing: 2px;
        }
        .coupon-header .title {
            font-size: 36px;
            font-weight: 900;
            color: #FFD700;
            margin: 10px 0 4px;
            text-shadow: 2px 2px 0 #b8860b;
        }
        .coupon-header .title-en {
            font-size: 14px;
            color: #ffeeba;
            display: block;
        }
        .coupon-header .discount-badge {
            display: inline-block;
            background: #FFD700;
            color: #c0392b;
            font-size: 48px;
            font-weight: 900;
            width: 100px;
            height: 100px;
            line-height: 100px;
            border-radius: 50%;
            position: absolute;
            top: 50%;
            right: 20px;
            transform: translateY(-50%);
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            text-shadow: 2px 2px 0 #e6c200;
        }
        .discount-badge .off {
            font-size: 18px;
            display: block;
            line-height: 0;
            margin-top: -20px;
        }
        /* 中间主体 */
        .coupon-body {
            padding: 20px 25px;
            border-bottom: 2px dashed #ddd;
        }
        .coupon-body .drink-icon {
            font-size: 40px;
            margin-bottom: 8px;
        }
        .coupon-body .offer-cn {
            font-size: 20px;
            font-weight: bold;
            color: #c0392b;
        }
        .coupon-body .offer-en {
            font-size: 13px;
            color: #888;
            display: block;
            margin: 4px 0 8px;
        }
        .coupon-body .detail {
            font-size: 13px;
            color: #555;
            line-height: 1.7;
            text-align: left;
            background: #fff8f8;
            border-radius: 12px;
            padding: 12px 16px;
            margin: 10px 0;
        }
        .coupon-body .detail .en {
            color: #999;
            font-size: 11px;
        }
        /* 底部 */
        .coupon-footer {
            padding: 16px 20px;
            background: #fff5f5;
        }
        .coupon-footer .store {
            font-size: 15px;
            font-weight: bold;
            color: #c0392b;
        }
        .coupon-footer .store-en {
            font-size: 11px;
            color: #999;
            display: block;
        }
        .coupon-footer .validity {
            display: inline-block;
            background: #ffe0e0;
            color: #c0392b;
            font-size: 13px;
            font-weight: bold;
            padding: 6px 14px;
            border-radius: 20px;
            margin: 8px 0;
        }
        .coupon-footer .validity-en {
            font-size: 10px;
            color: #999;
            display: block;
            margin-top: 2px;
        }
        .coupon-footer .note {
            font-size: 11px;
            color: #aaa;
            margin-top: 6px;
        }
        .coupon-footer .code {
            font-family: 'Courier New', monospace;
            font-size: 20px;
            font-weight: bold;
            color: #c0392b;
            letter-spacing: 3px;
            margin: 8px 0;
            background: #ffeaea;
            display: inline-block;
            padding: 4px 12px;
            border-radius: 8px;
        }
        /* 锯齿边 */
        .zigzag {
            position: relative;
            height: 20px;
            background: white;
            border-top: 2px dashed #ddd;
            overflow: hidden;
        }
        .zigzag::before {
            content: '';
            position: absolute;
            top: -11px;
            left: -10px;
            width: calc(100% + 20px);
            height: 20px;
            background: radial-gradient(circle at 0 0, transparent 10px, white 10px) 0 0,
                        radial-gradient(circle at 20px 0, transparent 10px, white 10px) 20px 0;
            background-size: 20px 20px;
        }
    </style>
</head>
<body>
    <div class="coupon">
        <!-- 顶部 -->
        <div class="coupon-header">
            <span class="brand">🍦 蜜雪冰城</span>
            <span class="brand-en">MIXUE ICE CREAM & TEA</span>
            <div class="title">第二杯半价</div>
            <span class="title-en">2nd Cup 50% Off</span>
            <div class="discount-badge">
                <span class="off">50%</span>OFF
            </div>
        </div>

        <!-- 中间 -->
        <div class="coupon-body">
            <div class="drink-icon">🧊🍋🫧</div>
            <div class="offer-cn">全场饮品 · 第二杯半价</div>
            <span class="offer-en">All Drinks · 2nd Cup Half Price</span>
            <div class="detail">
                ✅ 同价位或低价位饮品享半价<br>
                <span class="en">Lower or equal priced drink at 50% off</span><br><br>
                ✅ 可混搭任意产品<br>
                <span class="en">Mix & match any drinks</span><br><br>
                ✅ 不与其它优惠叠加<br>
                <span class="en">Cannot be combined with other promos</span>
            </div>
        </div>

        <!-- 锯齿分隔 -->
        <div class="zigzag"></div>

        <!-- 底部 -->
        <div class="coupon-footer">
            <div class="store">📍 蜜雪冰城 · 波动巴西店</div>
            <span class="store-en">MIXUE · Potong Pasir</span>
            <br>
            <div class="validity">
                📅 <span id="validDate"></span>
            </div>
            <span class="validity-en" id="validDateEN"></span>
            <br>
            <div class="code">优惠码：2ND50</div>
            <div class="note">
                请截图保存 · 到店出示即享优惠<br>
                <span style="font-size:10px">Screenshot & show in store to redeem</span>
            </div>
        </div>
    </div>

    <script>
        // 显示当天日期（有效期当天）
        const now = new Date();
        const year = now.getFullYear();
        const month = String(now.getMonth() + 1).padStart(2, '0');
        const day = String(now.getDate()).padStart(2, '0');
        const weekdaysCN = ['星期日','星期一','星期二','星期三','星期四','星期五','星期六'];
        const weekdaysEN = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];
        const wd = now.getDay();

        document.getElementById('validDate').textContent = 
            `有效期：${year}年${month}月${day}日 ${weekdaysCN[wd]}`;
        document.getElementById('validDateEN').textContent = 
            `Valid: ${year}-${month}-${day} ${weekdaysEN[wd]}`;
    </script>
</body>
</html>
